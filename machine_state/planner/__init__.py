"""Planning and Tool Contract Layer (Phase 6).

Provides structured, deterministic orchestration between user intent
and runtime execution. The LLM is never given direct system access —
it operates through this contract layer.
"""
